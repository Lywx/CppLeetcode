#include <string>

using namespace std;

// complexity O(N).
class Solution
{
public:
    int strStr(string haystack, string needle)
    {
        int m = int(haystack.size());
        int n = int(needle.size());

        return -1;
    }
};